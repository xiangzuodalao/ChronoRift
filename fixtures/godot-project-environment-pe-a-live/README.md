# PE-A live project

The main scene has one project-specific state concept: `Main.counter`. The
`project_state()` method returns its bounded serializable projection. This is a
test project, not a built-in ChronoRift adapter profile.
